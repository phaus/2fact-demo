image: phaus/play2build:8

stages:
  - build
  - test
  - publish
  - deploy

sbt:
  stage: build
  script:
    - sbt compile

test:
  stage: test
  script:
    - sbt test

dist:
  stage: publish
  script:  
    - sbt dist

heroku:
  stage: deploy
  script:
    - dpl --provider=heroku --app=a2fact-demo --api-key=$HEROKU_API_KEY
